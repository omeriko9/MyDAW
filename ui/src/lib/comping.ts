/**
 * Take-lane (versions) UI constants.
 *
 * The comp-segment maths that used to live here — compSegments/laneAt/paintComp, the
 * verbatim twin of the engine's paintCompRange — went away with the comp array on
 * 2026-08-11: audibility is per-clip mute now, so there is nothing to compute.
 */

export const LANE_COLORS = ["#4f8cff", "#28c07a", "#e0a533", "#c96be0", "#e0605f", "#33bcd6"];
